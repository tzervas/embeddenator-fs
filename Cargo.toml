[package]
name = "embeddenator-fs"
version = "0.1.0"
edition = "2021"
authors = ["Tyler Zervas <tz-dev@vectorweight.com>"]
description = "EmbrFS: FUSE-based holographic filesystem"
license = "MIT"
repository = "https://github.com/tzervas/embeddenator-fs"
keywords = ["embeddenator", "vsa", "holographic", "sparse-ternary"]
categories = ["algorithms", "data-structures", "memory-management"]

[features]
default = []
simd = []
block-sparse = []

[dependencies]
# Component dependencies will be minimal
# Add specific deps as needed per component

[dev-dependencies]
proptest = "1.0"
criterion = "0.5"

[[bench]]
name = "component_bench"
harness = false
